Implemented and committed the stage-4.1 QPBT skeleton.

Commit: `db4327a7c8f6eb5e5dbd3dc234267bd76cabaa97` (`feat(QPBT): add Pauli basis test skeleton`)

Created:

- [QPBT.lean](/Users/dengnifer/Documents/ghz/ldt/MIPStarRE-dev/.worktrees/issue-0006-qpbt-minimal-skeleton/MIPStarRE/QPBT.lean)
- Algebra modules: `Subspaces`, `FieldBasis`, `LowDegreeCode`, `Lines`, `Pauli`
- Game modules: `Defs`, `Distance`, `CondLinear`
- Test modules: `LowDegreeGame`, `MagicSquare`, `PauliBasisTest`, `Soundness`

Modified [MIPStarRE.lean](/Users/dengnifer/Documents/ghz/ldt/MIPStarRE-dev/.worktrees/issue-0006-qpbt-minimal-skeleton/MIPStarRE.lean) to re-export QPBT.

The complete 39-node statement closure is represented. The QPBT files contain 167 declarations including supporting definitions, instances, and helpers. There are exactly 16 `sorry` occurrences, all in proof positions; no axioms or kernel-bypass tokens were introduced.

Validation passed:

- Per-file `lake env lean` checks
- `lake build MIPStarRE.QPBT`: `8682/8682`
- Full `lake build`: `9005/9005`
- Forbidden-token and whitespace scans
- Pre-commit statement-origin, proof-debt, conclusion-shaped-hypothesis, and axiom audits
- Final worktree is clean

**Statement Integrity**

Paper assumptions: universal constants `a ≥ 1`, `0 < b < 1`; admissible `(q,m,d)`; positive error parameter; any strategy for the Pauli game with success at least `1 - ε`; no projectivity assumption.

Lean assumptions: these same conditions, represented by `AdmissibleParams` with positivity/divisibility fields and an explicit fixed `FixedFieldModel` carrying the binary representation and self-dual-normal-basis convention. The theorem has no arbitrary `[FieldModel P.q]` binder.

Paper conclusion: local isometries, a unit auxiliary state, closeness to auxiliary state tensor EPR registers, and state-dependent A/B Pauli operator-family approximation.

Lean conclusion: `PauliSoundnessWitness`, concrete finite-dimensional isometries and ideal state, a vector-norm bound, and explicit finite squared operator-distance sums. Verdict: faithful quantitative encoding with documented finite-dimensional and fixed-model boundary representations. The inherited `FieldModel.equiv` and added `binaryEquiv` are currently stored separately and should be identified explicitly before coordinate-heavy proofs.

Blueprint files were intentionally unchanged. Later synchronization still needs the missing trace/binary-representation `\uses` edges, typed-CL declarations (currently inlined), and the other adjudicated OPEN items; the expansion lemma and RREF equivalence remain deferred as specified.

## Session summary
Implemented and committed the 39-node QPBT stage-4.1 skeleton for issue #0006 / PR #0003 in session `orc-0006-20260831-01`; commit `db4327a` passed the full build and repository audits.

## Goal alignment
The requested `MIPStarRE.QPBT.pauli_soundness` statement and transitive closure are present, cited, imported, and type-checked.

## Loose ends
- Sixteen proof obligations remain intentionally deferred (`sorry`).
- Blueprint synchronization and the fixed-model equivalence identification remain pending.

## Unblocked follow-ups
- Prover: discharge the tracked stage-4.1 obligations.
- Blueprint: synchronize the documented QPBT declaration links and `\uses` gaps.

## Recommendation
Next: discharge the 16 tracked proof obligations, then synchronize the documented blueprint gaps (prover/blueprint).